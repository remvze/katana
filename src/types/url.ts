export interface Url {
  clicks: number;
  created_at: number;
  destruction_key: string;
  encrypted_url: string;
  hashed_slug: string;
  id: string;
  is_deleted: boolean;
  is_password_protected: boolean;
}
