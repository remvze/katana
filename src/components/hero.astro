---
import { Container } from './container';
---

<Container>
  <section class="hero">
    <img alt="Katana Logo" src="/logo.svg" />
    <h1>Katana</h1>
    <p>End-to-end encrypted URL shortener.</p>
  </section>
</Container>

<style>
  .hero {
    padding: 100px 0 40px;

    & img {
      display: block;
      width: 24px;
      margin-bottom: 16px;
    }

    & h1 {
      margin-bottom: 4px;
      font-family: var(--font-display);
      font-size: var(--font-xlg);
      font-weight: 700;
    }

    & p {
      color: var(--color-foreground-subtle);
    }
  }
</style>
